//prime or not
#include<stdio.h>
#include<conio.h>
int main()
{
	int factcount=0,i=1,n;
	printf("enter the num");
	scanf("%d",&n);
	while(i<=n)
	{
		if(n%i==0)
		factcount++;
		i++;
	}
	if(factcount==2)
	printf("prime number");
	else
	printf("not a prime number");
}
